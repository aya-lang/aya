.# Original implementation or remdups
.# Removes all duplicate elements from a list
.# Now replaced with :~

.# L remdups\n removes duplicates in a list
{l:set,
  []:set;

  l#{i,
    .# If the element is not already in the set, add it
    {}{
       set [i] K:set;
    } set i N(-1)= ?
    i
  };

  set
}:remdups;
